#version 330 core

// Ouput data
out vec4 fColor;
uniform vec3 ShapeColor;
in vec4 vertexColor; // the input variable from the vertex shader (same name and same type)  

void main()
{
	// Output color = red 
	fColor = vec4(ShapeColor,0.5);
	

}